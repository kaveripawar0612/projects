# Customer Churn Prediction

End-to-end machine-learning starter project for predicting customer churn.

### Pipeline
Data generation → preprocessing → train/test split → Logistic Regression → evaluation.

The included dataset is synthetic and model metrics are produced only when the script is run; no unverified accuracy is claimed in this README.

### Run
```bash
pip install -r requirements.txt
python train.py
```
